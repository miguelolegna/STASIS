import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { VitePWA } from 'vite-plugin-pwa';
import tailwindcss from '@tailwindcss/vite'; // ADICIONA ISTO

export default defineConfig({
  plugins: [
    tailwindcss(), // ADICIONA ISTO COMO PRIMEIRO PLUGIN
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.ico', 'apple-touch-icon.png', 'mask-icon.svg'],
      manifest: {
        name: 'Auditor Financeiro Pro',
        short_name: 'Auditor',
        description: 'Gestão Radical de Finanças e Dívidas',
        theme_color: '#021729',
        background_color: '#021729',
        display: 'standalone',
        orientation: 'portrait',
        icons: [
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          }
        ]
      }
    })
  ]
});